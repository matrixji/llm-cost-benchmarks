你是一个资深的软件工程师。请基于以下提供的 3 个初始文件内容，完成一个增量开发需求。

【增量需求描述】
1. 修改 `models.py`：为 VehicleData 结构体增加一个 `status_code` 字段（整数类型）。
2. 修改 `parser.py` 中的 `parse_message` 函数：
   - 如果输入 JSON 中包含 'status_code'，则直接解析。
   - 如果不包含，默认将 `status_code` 设为 0。
   - 增加一条边界业务逻辑：如果解析出的 `speed` 严格大于 120，则强制将 `status_code` 修改为 99（代表超速状态）。

【输出要求】
请直接给出修改后的 `models.py` 和 `parser.py` 的完整代码，无需输出多余的解释。





你是一个资深的软件工程师。请基于以下提供的最新文件内容，完成一个新增缓存模块的需求。

【增量需求描述】
我们需要在内存中对设备的状态变更进行过滤，只有当状态发生改变时才做处理。
1. 请新建一个文件 `cache_manager.py`，实现一个 `VehicleCache` 类（最好使用单例模式或简单的全局内存管理）。该类内部使用字典管理缓存，Key 为 `device_id`，Value 为最新的 `VehicleData` 对象。提供一个 `update_and_check_changed(data: VehicleData) -> bool` 方法：传入新的数据，如果该设备是首次登录，或者新数据的 `status_code` 与缓存中的旧数据不同，则更新缓存并返回 `True`；如果状态未改变，则返回 `False`。
2. 修改 `main.py`：引入这个缓存器，在循环/主流程中，只有当 `update_and_check_changed` 返回 `True` 时，才打印 "状态发生变更: [具体数据]"。

【输出要求】
请直接给出新建的 `cache_manager.py` 和修改后的 `main.py` 的完整代码。





你是一个资深的软件工程师。请基于以下提供的完整项目代码，使用 pytest 框架编写单元测试。

【增量需求描述】
请新建一个文件 `test_solution.py`，为现有系统编写完善的 pytest 单元测试。必须覆盖以下核心场景：
1. 测试 `parser.py`: 验证正常数据解析；验证当 `speed` 为 120（边界）和 121 时，`status_code` 是否如预期分别触发 0 和 99。
2. 测试 `parser.py` 的鲁棒性: 如果输入的 `raw_str` 是非法的 JSON 字符串（如损坏的数据），函数应该如何优雅处理而不让主程序崩溃？请在测试中体现这一异常保护。
3. 测试 `cache_manager.py`: 验证同一设备状态未变时返回 `False`，状态改变时返回 `True`；验证不同设备之间的缓存隔离。

【输出要求】
请直接给出 `test_solution.py` 的完整测试代码，以及如果为了支持异常保护需要对 `parser.py` 做出的轻微重构（如果有的话）。